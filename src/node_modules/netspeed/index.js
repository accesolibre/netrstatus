var config = require('config')
var speedtest = require('speedtest-net')

function run (callback) {
  var test = speedtest({ maxTime: config.netspeed.maxTime })

  test.on('data', function (data) { return callback(null, data) })

  test.on('error', function (err) { return callback(err) })
}
module.exports = run
